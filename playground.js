const e = Math.E
const pi = Math.PI
const cos = x => Math.cos(x*pi/180)
const sin = x => Math.sin(x*pi/180)
const tan = x => Math.tan(x*pi/180)
const arccos = x => Math.acos(x)*180/pi
const arcsin = x => Math.asin(x)*180/pi
const arctan = x => Math.atan(x)*180/pi

function F(x) {
    return Math.floor()
}

console.log(e**-(e**2))